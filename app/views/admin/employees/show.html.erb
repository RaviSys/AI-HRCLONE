<p style="color: green"><%= notice %></p>
<div class="col-lg-12">
  <div class="card rounded-0 mt-5">
    <div class="card-body">
      <p style="color: green"><%= notice %></p>
      <%= render @employee %>
      <div>
        <%= link_to "Back", admin_employees_path, class: "btn btn-outline-primary btn-lg" %>
      </div>
    </div>
  </div>
</div>
